-----------------------
det(M): Retorna o determinante da matriz M.
-----------------------
M': retorna a matriz transposta (troca linhas pelas colunas) de uma matriz;

x = [
      1, 2, 3;
      4, 5, 6;
      7, 8, 9
    ];
disp(x');
#    1   4   7
#    2   5   8
#    3   6   9
-----------------------
floor (X)
     Return the largest integer not greater than X.

 floor ([-2.7, 2.7])
 => -3    2
-----------------------
mod(x,y): retorna o resto da divisão de x por y.
-----------------------
mean(v): retorna o média dos elementos do vetor.
            retorna a média de cada colunna caso for uma matriz.

